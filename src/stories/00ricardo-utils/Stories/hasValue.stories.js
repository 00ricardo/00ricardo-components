import { HasValue } from '../Components/hasValue';

export default {
    title: '00ricardo-utils/Validation Functions',
    component: HasValue
};

export const hasValue = {
    args: {
        'dataType': 'String',
    },
};
