import { HasProperty } from '../Components/hasProperty';

export default {
    title: '00ricardo-utils/Validation Functions',
    component: HasProperty
};

export const _hasProperty = {
    args: {
        'property': '',
    },
};